{
  "name": "dev-landing",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "src"
  ],
  "scripts": {
    "dev": "npm --workspace src run dev",
    "build": "npm --workspace src run build",
    "start": "npm --workspace src run start"
  },
  "engines": {
    "node": ">=18.17.0"
  },
  "dependencies": {
    "next-pwa": "^5.6.0"
  }
}
